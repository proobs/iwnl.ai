import { FC, useState } from "react";

interface RankProps {
    rank: string;
    leaguePoints: number;
    wins: number;
    loss: number
}

const Rank:FC<RankProps> = ({ rank, leaguePoints, wins, loss}) => {
    return <></>
}